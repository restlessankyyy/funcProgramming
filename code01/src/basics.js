const  calories = 180;
// immutable data => The data won't change its state.

const  calories1 = 200;
const mealName = "Paneer";
const isEligible = true;
console.log(calories);

// immutable data => the data won't change its state. 

const calories2 = 1451;
console.log(calories2);

// object literals